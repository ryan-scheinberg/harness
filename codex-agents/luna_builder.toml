# Managed by harness codex-agents/install.sh
name = "luna_builder"
description = "Use for an implementation unit whose delegated prompt begins $role-build"
developer_instructions = "Use the role skill at the start of the delegated prompt; that skill owns your behavior"
model = "gpt-5.6-luna"
model_reasoning_effort = "xhigh"
